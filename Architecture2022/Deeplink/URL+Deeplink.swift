import Foundation

extension URL {
    static var profileDetails: URL {
        URL(string: "app://profileDetails")!
    }

    static var editProfile: URL {
        URL(string: "app://editProfile")!
    }
}
