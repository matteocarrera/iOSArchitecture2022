title: TokensResponse
description: "Авторизационные токены"
type: object
properties:
  accessToken:
    $ref: "../schemas/Token.yaml"
  refreshToken:
    $ref: "../schemas/Token.yaml"
required:
  - accessToken
  - refreshToken
